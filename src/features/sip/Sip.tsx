import React from 'react';
import StyledPage from '../styles/StyledPage';
import StyledTitle from '../styles/StyledTitle';

const Phone = () => {
  return (
    <StyledPage>
      <StyledTitle>Домофон</StyledTitle>
    </StyledPage>
  );
};

export default Phone;
